# font_ko.c 에 구울 한글 음절의 출처(source of truth).
#
# 이 파일이 "진짜" 목록이고, font_ko.c 의 --symbols 문자열은 이 파일에서
# gen-symbols.py 가 계산해서 만든다. 손으로 --symbols 문자열을 옮겨 적다가
# 다른 한자(器)가 섞여 들어간 적이 있어서(Task 13 브리프 리뷰, T13-A) 이후로는
# 이 파일 → 스크립트 → 생성물 순서만 쓴다.
#
# 새 라벨을 추가할 때: 여기에 단어를 한 줄(또는 기존 줄에 공백으로 구분해)
# 추가하고 tools/build-font.sh 를 다시 돌리면 된다. `#` 로 시작하는 줄과
# 빈 줄은 무시된다. 이 파일 자체에는 순서/그룹에 의미가 없다 — 사람이 읽기
# 쉬우라고 Task 13 브리프 Step 1 의 묶음을 그대로 유지했을 뿐이다.

# 연결 상태
연결됨 연결중 끊김 대기 활동 중 유휴 휴면

# 카운트다운 · 시간 표시
남음 시간 분 초 뒤 초기화 사용률 주간

# 페어링 화면
페어링 코드 입력 남은 시도 회 확인

# 페어링 실패 (Task 14b) — 시도 소진 시 안내 문구
# "맥에서 페어링을 다시 시작하세요" 전체를 그대로 옮겨 적는다 — 손으로
# 음절만 추리다가 빠뜨리는 사고(T13-A 재발 방지)를 피하려면 이 파일 자체가
# 화면에 뜨는 문장의 출처여야 한다.
맥에서 페어링을 다시 시작하세요

# 설정 · 오류
설정 기기 오류 없음
